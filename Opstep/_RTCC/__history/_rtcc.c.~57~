#include <_rtcc.h>

void main(){
   
   sys_init();
   //idle();
  while(TRUE) {
  idle();
  delay_ms(800);
  idle();
  
  
  //set_days();
     //switch(menu_state) {
      // case 7:
        // set_years();
        // break;
      // }
   }
}
