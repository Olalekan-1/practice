#include <_rtcc.h>

void main(){
   
   sys_init();
   //idle();
  while(TRUE) {
  //set_days();
     switch(menu_state) {
       case 7:
         set_years();
         break;
       }
   }
}
