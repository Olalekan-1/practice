#include <_rtcc.h>

void main(){
   
   sys_init();
   //idle();
  while(TRUE) {
  //set_days();
     switch(menu_state) {
       case 6:
         set_months();
         break;
       }
   }
}
