#include <timer.h>

void main()
{
   sys();
   while(TRUE)
   {
    if(input(inc)){
      while(input(inc));
      biggi++;
      if(biggi > 99) {
         biggi == 99;
       }
    }
    if(input(dec)){
      while(input(dec));
      
      
    }
   }

}
