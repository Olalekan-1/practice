#include <16F84A.h>

#FUSES XT,NOWDT,PUT,PROTECT
#use delay(crystal=4000000)

#int_ext
void isrext(){
unsigned int8 y = 0;
   clear_interrupt(int_ext);
   for(y = 0; y < 7; y++){
      output_high(pin_a0);
      delay_ms(500);
      output_low(pin_a0);
      delay_ms(500);
   }
}
